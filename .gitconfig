[user]
	name = Rogin Farrer
[color]
	ui = true
	status = always
[push]
	default = current
[fetch]
	prune = true
[core]
	autocrlf = false
	preloadindex = true
	fscache = true
	excludesfile = /Users/rfarrer/.gitignore_global
	editor = nvim
	symlinks = true
[log]
	decorate = short
[http]
	sslVerify = false
[pack]
	useSparse = true
[hub]
	host = github.csnzoo.com
[merge]
       tool = vscode
[mergetool "meld"]
    # Choose one of these 2 lines (not both!) explained below.
    cmd = meld "$LOCAL" "$MERGED" "$REMOTE" --output "$MERGED"
    ; cmd = meld "$LOCAL" "$BASE" "$REMOTE" --output "$MERGED"
[mergetool "vscode"]
        cmd = code --wait $MERGED
[mergetool]
	keepBackup = false
 	prompt = false
 	trustExitCode = true
